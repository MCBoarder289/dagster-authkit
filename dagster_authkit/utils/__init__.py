"""
Utility modules for dagster-authkit.

Contains:
- config.py    — AuthConfig (reads all env vars; validates)
- audit.py     — Structured JSON audit logging to stdout
- logging.py   — Configures the dagster_authkit logger
- templates.py — HTML page templates (login, 403, user menu injection)
- display.py   — Terminal banners and config summaries
"""
